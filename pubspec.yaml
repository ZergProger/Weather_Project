name: weather_project
description: "A new Flutter project."

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.7.0
  jiffy: ^5.0.0
  flutter_bloc: ^9.0.0
  get_it: ^8.0.3
  bloc: ^9.0.0
  flutter_bounce: ^1.1.0
  bounce: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:

  uses-material-design: true